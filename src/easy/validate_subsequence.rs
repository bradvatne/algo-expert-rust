use std::num;


pub fn validate_subsequence(arr: &[i32], sub: &[i32]) -> bool {
    let mut pos = arr.length()-1;
    let mut i = 0;
    let mut j = arr.length()-1;
    let newArr = vec!(arr.length());
    if (num::abs(nums[i] < num::abs(nums[j]))) {
        
    }

}

fn main() {
    let test = vec![5, 1, 22, 25, 6, -1, 8, 10];
    let testSub = vec![1, 6, -1, 10];

}